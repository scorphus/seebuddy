# Copy to .env.local for local overrides, or set in your host's build env.
# Empty/unset in dev — the Vite proxy in vite.config.ts forwards /lakes to
# the local Encore daemon at :4000.
VITE_API_BASE_URL=
